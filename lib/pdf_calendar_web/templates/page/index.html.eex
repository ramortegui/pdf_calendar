<h1>PdfCalendar</h1>
<%= form_for @changeset, Routes.page_path(@conn, :download), fn f -> %>
  <%= select f, :month, months(), [selected: current_month()] %>
  <%= select f, :year, years(), [selected: current_year()] %>
  <%= submit "Download" %>
<% end %>
